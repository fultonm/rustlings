// variables5.rs
// Make me compile! Execute the command `rustlings hint variables5` if you want a hint :)

// I'M NOT DONE
fn main() {
    let mut number: String = "T-H-R-E-E".into();
    println!("Spell a Number : {}", number);
    let number: u32 = 3;
    println!("Number plus two is : {}", number + 2);
}
